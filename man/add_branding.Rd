% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_branding.R
\name{add_branding}
\alias{add_branding}
\title{Add Complete Branding to ggplot2 Charts}
\usage{
add_branding(
  github = NULL,
  gitlab = NULL,
  linkedin = NULL,
  bluesky = NULL,
  twitter = NULL,
  mastodon = NULL,
  orcid = NULL,
  email = NULL,
  website = NULL,
  custom_icons = NULL,
  additional_text = NULL,
  text_position = "before",
  line_spacing = 1L,
  icon_color = "#666666",
  text_color = "#333333",
  use_brand_colors = FALSE,
  icon_size = "8pt",
  text_size = "8pt",
  line_height = "1.2",
  text_family = "sans",
  setup_fonts = TRUE,
  caption_width = NULL,
  caption_halign = 0,
  caption_margin = NULL
)
}
\arguments{
\item{github}{(\code{NULL | character(1)}) GitHub username (optional)}

\item{gitlab}{(\code{NULL | character(1)}) GitLab username (optional)}

\item{linkedin}{(\code{NULL | character(1)}) LinkedIn username (optional)}

\item{bluesky}{(\code{NULL | character(1)}) Bluesky handle (optional)}

\item{twitter}{(\code{NULL | character(1)}) Twitter/X handle (optional)}

\item{mastodon}{(\code{NULL | character(1)}) Mastodon handle (optional)}

\item{orcid}{(\code{NULL | character(1)}) ORCID ID (optional)}

\item{email}{(\code{NULL | character(1)}) Email address (optional)}

\item{website}{(\code{NULL | character(1)}) Website URL (optional)}

\item{custom_icons}{(\code{NULL | character}) Named vector of additional icon names and usernames (optional). Names should match icons from icons_df.}

\item{additional_text}{(\code{NULL | character(1)}) Additional text to include in caption (e.g., "Data source: XYZ")}

\item{text_position}{(\code{character(1)}) Position of additional text relative to branding. Either "before" or "after". (default: "before")}

\item{line_spacing}{(\code{integer(1)}) Number of line breaks between additional text and icons (1-3). (default: 1)}

\item{icon_color}{(\code{character(1)}) Color for icons. (default: "#666666")}

\item{text_color}{(\code{character(1)}) Color for usernames/text. (default: "#333333")}

\item{use_brand_colors}{(\code{logical(1)}) Whether to use brand-specific colors for icons. (default: FALSE)}

\item{icon_size}{(\code{character(1)}) Font size for icons. (default: "8pt")}

\item{text_size}{(\code{character(1)}) Font size for text/usernames. (default: "8pt")}

\item{line_height}{(\code{character(1)}) Line height for the caption. (default: "1.2")}

\item{text_family}{(\code{character(1)}) Font family for text/usernames. (default: "sans")}

\item{setup_fonts}{(\code{logical(1)}) Whether to automatically setup Font Awesome fonts. (default: TRUE)}

\item{caption_width}{(\code{NULL | character(1)}) Width specification for caption text box (default: NULL uses ggtext default)}

\item{caption_halign}{(\code{numeric(1)}) Horizontal alignment for caption (0 = left, 0.5 = center, 1 = right). (default: 0)}

\item{caption_margin}{(\code{NULL | ggplot2::margin}) Margin specification for caption (default: NULL uses ggtext default)}
}
\value{
(\code{list}) A list of ggplot2 components that can be added with \code{+}
}
\description{
This function creates a ggplot2 layer that can be added with \code{+} to apply
both theme styling (using element_textbox_simple from ggtext) and branded
captions to your ggplot2 charts.
}
\examples{
\dontrun{
library(ggplot2)
library(ggtext)
library(ggbranding)

# Use with + in ggplot chain
ggplot(mtcars, aes(x = mpg, y = wt)) +
  geom_point() +
  labs(title = "My Plot") +
  add_branding(
    github = "yourusername",
    linkedin = "yourprofile"
  )

# With additional text
ggplot(iris, aes(x = Sepal.Length, y = Sepal.Width)) +
  geom_point() +
  add_branding(
    github = "yourusername",
    additional_text = "Data source: iris dataset",
    text_position = "before"
  )

# With custom icons and styling
ggplot(mtcars, aes(x = mpg, y = wt)) +
  geom_point() +
  add_branding(
    github = "yourusername",
    custom_icons = c(
      envelope = "contact@example.com",
      globe = "https://mywebsite.com"
    ),
    use_brand_colors = TRUE,
    caption_halign = 1
  )
}

}
